import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angular-virtual-scroll-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
